<style>
  .root {
    display: flex;
    flex-direction: column;
    align-items: center;

    min-height: 100dvh;
    /* padding-bottom: var(--u3); */

    background-color: black;

    .title {
      width: min(calc(40 * var(--u2)), calc(100vw - 2 * var(--u3)));
      transform: translateY(50%);
    }

    .content {
      flex: 1;
      width: 100%;
      max-width: var(--page-width);

      background-color: var(--gray-2);

      display: flex;
      flex-direction: column;

      padding: var(--u6);
      gap: var(--u5);

      box-sizing: border-box;
    }
  }
</style>

<div class="root">
  <!-- <img class="title" src="/assets/title.png" /> -->

  <main class="content">
    <slot />
  </main>
</div>
