<style>
  .root {
    display: flex;
    flex-direction: column;
    align-items: center;

    min-height: 100dvh;
    /* padding-bottom: var(--u3); */

    background-color: var(--gray-1);

    .title {
      width: min(calc(40 * var(--u2)), calc(100vw - 2 * var(--u3)));
      transform: translateY(50%);
    }

    .wrapper {
      flex: 1;
      width: 100%;
      max-width: var(--page-width);

      display: flex;

      padding: var(--u0);
      padding-bottom: 0;
      box-sizing: border-box;

      background: linear-gradient(90deg, var(--gray-5), var(--gray-3));

      .content {
        flex: 1;

        background-color: var(--gray-2);

        display: flex;
        flex-direction: column;

        padding: var(--u4);
        padding-top: calc(var(--u4) + var(--u3));
        gap: var(--u3);

        box-sizing: border-box;
      }
    }
  }
</style>

<div class="root">
  <img class="title" src="/assets/title.png" />

  <div class="wrapper">
    <main class="content">
      <slot />
    </main>
  </div>
</div>
